## 무엇

<!-- 이 PR이 하는 변경을 한두 문단으로. -->

## 왜

<!-- 해결하는 문제/맥락. 관련 ROADMAP 항목·이슈·ADR 링크.
     대응 이슈가 있으면 반드시 `Closes #N` — 머지 시 이슈가 자동으로 닫힌다. -->

## 검증

<!-- 어떻게 확인했는가. 통과한 테스트(모듈·개수), 라이브 스모크 여부.
     BUILD SUCCESSFUL 만이 아니라 실제 tests/failures 수치로. -->

## 체크리스트

- [ ] 아키텍처 규칙 준수(헥사고날 계층·도메인 간 직접 의존 없음 — `ArchUnit` 통과)
- [ ] 영향 모듈 `:test` + 필요 시 `:carry-app:test`(Testcontainers) 통과
- [ ] 이벤트/스키마 변경 시 가산적 호환(또는 새 eventType) — [ADR-0005] 준수
- [ ] base 브랜치 = `develop`
- [ ] 대응 이슈가 있으면 본문에 `Closes #N` 포함
- [ ] 후속(범위 밖) 작업이 있으면 본문에 명시

## 후속 (범위 밖)

<!-- 의도적으로 미룬 작업이 있으면 적는다. 없으면 "없음". -->
